{
  "name": "act_group63",
  "version": "1.0.0",
  "description": "",
  "main": "server/base.py",
  "scripts": {
    "start": "node server/server.js --ignore client",
    "develop": "concurrently \"server/venv/Scripts/activate && flask run\" \"cd client && npm start\"",
    "flask": "/server/venv/Scripts/activate",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
